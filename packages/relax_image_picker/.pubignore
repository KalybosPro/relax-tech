# Files kept in git but left out of the published archive.

# Promo material: ~4 MB of images every consumer would download on pub get.
assets/promo/

# A .pubignore takes precedence over the .gitignore of the same directory, so
# the dev-only entries from it have to be repeated here.
*.iml
*.ipr
*.iws
.idea/
.dart_tool/
.flutter-plugins-dependencies
/build/
/coverage/
/pubspec.lock
**/doc/api/
